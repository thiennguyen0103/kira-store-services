syntax = "proto3";

package products;

service ProductsService {
  rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {}

message PingResponse {
  bool ok = 1;
  string service = 2;
}
