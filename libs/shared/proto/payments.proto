syntax = "proto3";

package payments;

service PaymentsService {
  rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {}

message PingResponse {
  bool ok = 1;
  string service = 2;
}
